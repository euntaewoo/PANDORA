# [안티그래비티 2.0] 제미나이 3.x 지시사항 누락(Semantic Drift) 방지 및 출력 생성제어 4대 안전장치 기술규격서
> **문서 상태**: 공식 기술 규격서 및 프롬프트 아키텍처 가이드  
> **최종 제정일**: 2026년 9월 3일  
> **적용 기준**: Antigravity 2.0 / Gemini 3.1 Pro Preview & Gemini 3.1 Flash Image  

---

## 1. 개요 및 배경 (Overview & Root Cause)

제미나이 3.7 / 3.8 등 최신 복합 추론(Thinking) 모델 환경에서 **"작업이 완료되었다고 답변하면서도, 실제 최종 출력 코드나 문서에서 사용자의 프롬프트 지시사항이 대거 누락되거나 기존 코드로 회귀하는 현상"**은 Attention 포화 및 지시 노화(Semantic Drift)에 의해 발생합니다.

### 📌 근본 기술 메커니즘
1. **토큰 버짓 잠식 (Token Budget Collision)**:
   - 최신 추론 모델의 출력 한도(`max_output_tokens`)에는 내부 추론 토큰(Thinking Tokens)과 최종 응답 토큰(Response Tokens)이 공유됩니다.
   - 복잡한 코드 분석 시 추론 블록이 수천 토큰을 소모하면, 모델은 남은 토큰 한도 내에서 강제로 출력을 마쳐야 하므로 코드 후반부 지시사항을 자의적으로 생략(`// 기존 코드 유지`, `...`)하거나 단순화합니다.
2. **추론 루프 내 셀프 컨펌 편향 (Self-Confirmation Bias)**:
   - 모델이 내부 추론(Thinking) 단계에서 *"사용자가 요구한 A 조건을 반영해야 한다"*고 서술한 뒤, 이미 해당 요구사항을 완수했다고 자체 판단하여 정작 본문 출력 단계에서는 기본 사전학습 패턴(흔한 코드)으로 회귀합니다.

---

## 2. 출력 생성 과정 제어 4대 핵심 안전장치

### ① [해결책 1] 축약/생략 표현 하드밴 (Lazy Coding Hard-Ban)
모델이 사용하는 게으른 코딩 패턴을 시스템 레벨에서 원천 금지합니다.
```text
[엄격한 금지 규칙 - HARD BAN]
1. 다음 표현을 출력 코드 내에 포함하는 것을 전면 영구 금지한다:
   - "// ... 기존 코드와 동일 ..."
   - "// ... 나머지 로직 구현 ..."
   - "# 기존 코드 유지", "TODO:", "PASS", "..."
2. 작업이 완료되었다고 서술하면서 실제 코드 블록에서 해당 구현이 누락되는 경우,
   시스템 오류로 간주하여 답변을 처음부터 다시 생성한다.
```

### ② [해결책 2] 역검증 매트릭스(Pre-execution Checklist) 선행 출력 의무화
코드를 작성하기 전, 본인이 지켜야 할 체크리스트를 표(Table)로 직접 대조하게 만들어 자기 수정(Self-Correction) 메커니즘을 강제 발동합니다.
```text
반드시 아래 2단계 순서로 답변을 작성하십시오. 1단계를 건너뛰고 코드를 바로 작성하면 실패로 간주합니다.

### 1단계: 지시사항 이행 매트릭스
코드를 작성하기 전, 사용자의 요구사항을 아래 표 형식으로 누락 없이 작성하십시오.
| 번호 | 요구사항/제약조건 | 반영 여부 (O/X) | 적용할 구체적 구현 방식 |
| :--- | :--- | :--- | :--- |
| 1 | (요구사항 1) | O | (구체적 구현 설명) |
| 2 | (요구사항 2) | O | (구체적 구현 설명) |

### 2단계: 최종 코드 출력
위 매트릭스에서 'O'로 검증된 내용만 온전히 반영하여 완성형 코드를 작성하십시오.
```

### ③ [해결책 3] Diff (Search/Replace) 포맷 강제 (전체 재출력 금지)
수백~수천 줄의 전체 코드를 다시 작성하게 하면 Attention 메커니즘 특성상 중간 제약 조건이 필연적으로 유실됩니다. 수정 작업 시에는 Aider 스타일의 Search/Replace 블록만 출력하도록 강제합니다.
```text
[출력 규칙: 전체 코드 재출력 금지]
완성된 전체 소스 코드를 불필요하게 다시 작성하지 마십시오.
오직 수정이 필요한 블록에 대해 아래의 SEARCH/REPLACE 형식만 정확히 출력하십시오.

<<<<<<< SEARCH
(수정 전 원본 코드 블록)
=======
(지시사항이 정확히 반영된 수정 후 코드 블록)
>>>>>>> REPLACE
```
*효과*: 토큰 소모량이 90% 이상 절감되어 토큰 부족에 의한 지시사항 누락이 원천 차단됩니다.

### ④ [해결책 4] Thinking Budget 상한 제어 (Python SDK & AI Studio)
추론에 토큰이 무제한으로 소모되어 실제 출력이 잘리는 것을 방지하기 위해, `thinking_budget` 상한을 지정하고 출력 한도를 최대로 설정합니다.

```python
from google import genai
from google.genai import types

client = genai.Client()
response = client.models.generate_content(
    model="gemini-3.1-pro-preview",  # 프로젝트 공식 표준 모델
    contents=prompt,
    config=types.GenerateContentConfig(
        max_output_tokens=8192,
        thinking_config=types.ThinkingConfig(
            thinking_budget=1024  # 추론 토큰 상한 강제 (본문 토큰 잠식 차단)
        ),
        temperature=0.6,          # 프로젝트 표준 온도
        top_p=0.9
    )
)
```

---

## 3. 실전에 즉시 복사해 적용하는 마스터 프롬프트 템플릿

```markdown
[작업 목표]
(원하는 작업 목표 1줄 요약)

[필수 준수 지시사항]
1. (핵심 요구사항 A)
2. (핵심 요구사항 B)
3. (핵심 요구사항 C)

[참조 코드 / 입력 데이터]
```(기존 소스 코드 또는 데이터 입력)```

[출력 프로토콜 - 필수 준수]
1. 코드 작성에 앞서, 상기 [필수 준수 지시사항]이 코드 어디에 어떻게 적용되었는지 표(체크리스트)로 먼저 작성할 것.
2. 코드는 축약 표현(// 기존 내용 유지, ... 등) 없이 변경되는 블록(Search/Replace) 또는 완성형 파일 단위로 온전히 제공할 것.
```
---

## 4. [PRE-EXPORT-INTEGRITY-VERIFICATION-LOCK] 결과물 내보내기 전 사전 무결성 검증 및 리포트 강제

1. **[MANDATORY-GATE] 사전 검증 게이트 무조건 실행**:
   - 결과물 파일(.png, .html, .docx, .txt, .md 등)을 생성·저장·보고하기 전, 데이터 무결성과 포맷 규격을 체크하는 검증 함수(pre_export_integrity_check) 및 린터를 무조건 실행해야 합니다.
2. **[REPORT-FIRST] 데이터 무결성 요약 리포트 선-출력 의무화**:
   - 에이전트는 최종 결과물이나 파일 링크를 사용자에게 제시하기 전, 반드시 응답 상단에 ### 📋 [DATA-INTEGRITY-SUMMARY-REPORT] 요약 리포트 표(포맷 무결성, 콩글리시/금지어 0건 여부, 수치 일치성, 4종 파일 생성 여부)를 먼저 출력하여 검증 결과를 입증해야 합니다. 이 리포트 출력이 누락된 답변은 즉시 무효로 간주합니다.
3. **[GLOBAL-COMPLIANCE] 영미권/글로벌 뷰티 표준 명칭 강제**:
   - 무자극/저자극: 한국 성적서 0.00 직역투 배제 -> Hypoallergenic & Dermatologist-tested for sensitive skin 표준 강제.
   - 피부톤 케어: 'Tone Care / Dark Spot & Tone Care' 콩글리시 배제 -> Dark Spot & Discoloration Defense 표준 강제.
